# partyserver pins @cloudflare/workers-types ^4 while current wrangler wants
# ^5.  The package is TypeScript declarations only -- nothing here compiles
# TypeScript -- so the strict resolver is refusing over a file we never read.
legacy-peer-deps=true
